# Everything the image must not carry. `.local` is first for a reason: it holds a
# real deployment's owner password hash and live agent tokens.
.local
.env
.env.*
!.env.example

node_modules
build
.svelte-kit
coverage
data
test-results
playwright-report
blob-report

# The Claude Code plugin ships to agents, not into the server image.
plugins

.git
.github
.vscode
docs
*.db
*.db-shm
*.db-wal
