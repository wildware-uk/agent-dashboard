/**
 * Public entry point for the MCP server.
 *
 * `src/http/` mounts this at `POST /mcp`; nothing else should import it.
 *
 * Filled in by the `src/mcp/` slice (design §11 step 5). See ./README.md.
 */
export {};
