/**
 * Public entry point for the event bus.
 *
 * Other modules import from `$events`, never from a file inside it.
 *
 * Filled in by the `src/events/` slice (design §11 step 3): typed pub/sub plus
 * the ring buffer. See ./README.md for the boundary rules.
 */
export {};
